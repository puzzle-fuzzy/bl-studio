node_modules/
.turbo/
dist/
var/
coverage/
*.log
.DS_Store

# 本地环境文件（绝不提交）
infra/env/.env
infra/env/.env.test
infra/env/.env.production
infra/env/.env.prod-infra
infra/env/.env.prod-backup
.env
.env.local
.env.*.local

# 本地工具状态
.codegraph/
.superpowers/

# 部署镜像导出 tar（deploy-prod.sh / web-only 发版的临时产物，失败时也绝不应入库）
images-*.tar
web-*.tar

# Playwright
playwright-report/
test-results/
e2e/.playwright

# 本地 rehearsal 用的静态 ffmpeg/ffprobe（fetch-static-ffmpeg.sh 下载，不入库）
infra/ffmpeg/
