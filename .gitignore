node_modules/
.turbo/
dist/
var/
coverage/
*.log
.DS_Store

# 本地环境文件（绝不提交；模板例外）
.env
.env.*
!.env.example
!.env.test.example
!.env.production.example
!.env.prod-infra.example
!.env.prod-backup.example
.env.local
.env.*.local

# 本地工具状态
.codegraph/
.superpowers/

# 部署镜像导出 tar（deploy-prod.sh / web-only 发版的临时产物，失败时也绝不应入库）
images-*.tar
web-*.tar

# Playwright
playwright-report/
test-results/
tests/e2e/.playwright
