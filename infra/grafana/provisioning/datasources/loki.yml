# Grafana Loki 数据源（provisioning）。uid 固定为 lokiuid，供 dashboards 引用。
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: lokiuid
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000
